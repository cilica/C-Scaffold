#include <add_numbers.h>
#include <stdio.h>

int main()
{
   char a = 3;
   char b = 9;
   // printf() displays the string inside quotation
   printf("Hello, World! %d \n", SumOfTwoNumbers(a, b));

   return 0;
}